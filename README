
deps:
    openssl 1.1.1d or higher (important)
    opus/libopus 1.3-1 or higher
    cmake
    make
    gcc
    libpq (only needed if building with server)

to build default (library, tests, clients):
    ./configure
    cd bin && make

to build server:
    ./configure
    cd bin && make server

to build and install library globally
    ./configure
    cd bin && make
    sudo make install
